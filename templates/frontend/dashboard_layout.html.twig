<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>{% block title %}{% endblock %} - Dilcam</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" crossorigin="anonymous">

    <script src="https://code.jquery.com/jquery-3.3.1.min.js"  crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

</head>
<body>

    <div class="container">

        <div class="row">
            <div class="col-md-3">

                <a href="{{ path('home') }}">Dilcam</a>
                <br>
                {{ app.user.email }}
                <br>
                <a href="{{ path('security_logout') }}">Déconnexion</a>
                <br>

                <div class="list-group mt-4">
                    <a href="{{ path('dashboard_subscription_manage') }}" class="list-group-item list-group-item-action">
                        Gérer votre abonnement
                    </a>
                    <a href="{{ path('dashboard_change_password') }}" class="list-group-item list-group-item-action">
                        Changer votre mot de passe
                    </a>
                    <a href="{{ path('dashboard_transactions') }}" class="list-group-item list-group-item-action">
                        Vos paiements
                    </a>
                    <a href="{{ path('dashboard_alarm') }}" class="list-group-item list-group-item-action">
                        Activer / Désactiver l'alarme
                    </a>
                    <a href="{{ path('dashboard_shoot_junction') }}" class="list-group-item list-group-item-action">
                        Filmer
                    </a>
                    <a href="{{ path('dashboard_watch_watch') }}" class="list-group-item list-group-item-action">
                        Regarder
                    </a>
                    <a href="{{ path('dasboard_browse') }}" class="list-group-item list-group-item-action">
                        Visionner les photos
                    </a>
                </div>
            </div>
            <div class="col-md-9">
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success">
                        {{ message | raw}}
                    </div>
                {% endfor %}
                {% for message in app.flashes('danger') %}
                    <div class="alert alert-danger">
                        {{ message | raw }}
                    </div>
                {% endfor %}

                {% block body %}{% endblock %}
            </div>
        </div>



    </div>
</body>
</html>